# ADS1256 SPI ADC Extension with PICO_MODE=3

## TODO

- Make sure since we're only doing single-ended that we get all 21 bits of _postiive_ range, don't send a sign bit and waste that precious ENOB.
- Make sure scale and offset are returned correctly including PGA and bugger.
